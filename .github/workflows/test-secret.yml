name: Test Secret

on:
  workflow_dispatch:   # lets you run manually from Actions tab

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Print secret length
        run: echo "Length=$(echo -n '${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}' | wc -c)"
